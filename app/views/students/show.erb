<p>Welcome student.</p>

<% if @spots.empty? %>
  <p>There are currently no available session next week.</p>
<% else %>
  <p>Choose session(s) to register</p>
  <% @spots.each do |spot| %>
    <p>A session <%= spot.course_name %> at <a href="/students/spot/<%= spot.id %>"><%= spot.scheduled_at %></a></p>
  <% end %>
<% end %>